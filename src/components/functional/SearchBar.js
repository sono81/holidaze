import React from "react";

function SearchBar() {
  return (
    <input
      type="text"
      className="searchbar"
      placeholder="Find a place to stay..."
    ></input>
  );
}

export default SearchBar;
